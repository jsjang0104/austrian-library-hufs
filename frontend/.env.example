# 이 파일을 .env 로 복사한 뒤 값을 채우세요.
#   cp .env.example .env
#
# 주의: VITE_ 접두사가 붙은 변수는 빌드 시 JS 번들에 그대로 포함되어
# 브라우저에서 누구나 볼 수 있습니다. 비밀 값은 절대 넣지 마세요.

# 백엔드 API 주소 (미설정 시 http://localhost:8000 사용)
VITE_API_URL=http://localhost:8000
